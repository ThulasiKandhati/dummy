# Core speech recognition
faster-whisper>=0.10.0

# Audio processing (PyAudio will work in container with system deps)
pyaudio>=0.2.11
numpy>=1.21.0

# Optional: For web requests
requests>=2.28.0
SpeechRecognition>=3.10.0
google-generativeai==0.3.2
python-dotenv==1.0.0
